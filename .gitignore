# Terraform
**/.terraform/
# .terraform.lock.hcl is version-locked — commit it (do NOT ignore)
*.tfstate
*.tfstate.*
*.tfplan
plan.out
crash.log
crash.*.log
terraform.tfvars
*.auto.tfvars
!terraform.tfvars.example

# Secrets
pull-secret.txt
pull-secret.json
*.pem
!keys/*.pub
kubeconfig
kubeadmin-password
auth/
*.token
.env
.env.*
env.sh
**/install-config.yaml   # rendered from template, contains pull-secret

# Ansible
*.retry
inventory/lab.yml
!inventory/lab.yml.example

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
.docs/
